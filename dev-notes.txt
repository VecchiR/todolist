na verdade: IIFE?

const pageRenderer = (function () {
    // e tal
    // mas talvez fazer isso depois, pq possivelmente vai incluir a
    //função "renderPage(), mas tambem mt do "DOMstuff"
})();


function renderPage() {   

    "modular" -> recarregar certas porçoes da pagina de acordo com 
    o que foi feito
        dependendo da ação, nao precisa recarregar o main-content OU
        a sidebar
    
    render sidebar:
        filtros->
        projestos-> projectList (all entries)
    
    render main-content:
        quando carrega a pagina
            label-> default (all tasks)
            tasks-> taskList (all entries)

        quando clica em algum filtro
            label-> filtro selecionado
            tasks-> depende da condição do filtro

        quando clica em projeto
            label-> projeto selecionado (pesquisa pelo ID na projectList -> retrieve project.name)
            tasks-> na taskList, procura por tasks cujo task.project === retrive project.name(project.id) ->>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>> NA VERDADE, ADICIONAR PROJECT ID COMO PROPERTY DA TASK!

        quando cria/deleta projeto
        quando cria/deleta task
            label-> mantem selecionado o projeto/filtro que já tava
            tasks->tambem mantem (adiciona)

}



alem disso, não ta passando os id's pra pagina né


renderPage
    render sidebar
        render filter list
        render project list
    render main content
        render label DEFAULT (ALL TASKS)
        render main content (DEFAULT ALL TASKS)


ou seja, o main content depende do label
o label depende 
    se inicio -> all tasks
    o que foi clicado
        filter?
        projecto?


function updateTaskList () {

    remove old content

    criar um array de quais tasks mostrar
        DEPENDE DO FILTRO OU PROJETO    

    cria os elementos 
        DEPENDE SE É FILTRO OU PROJETO
        (se terá o campo do projeto)
}


ou seja: os filtros e projetos precisam de EVENT HANDLERS



hey -> o projectID nao precisa estar no html da task. Com o task ID conseguimos dar um
'filter' ou achar o projectID la na taskList quando for necessário